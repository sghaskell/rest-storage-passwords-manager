---
phase: 01.5-build-deployment
plan: 02
subsystem: testing
tags: [playwright, docker, e2e, crudsplunk-rest-api]
requires:
  - phase: 01.5-01 (webpack hardening)
    provides: working build pipeline and bundle.jsx entry point

# Dependency graph
provides:
  - Playwright config with Docker target URL (SPLUNK_HOST/SPLUNK_PORT env vars)
  - CRUD E2E test suite: credential list, create, update, delete
  - Error scenario tests: auth failure redirect, duplicate conflict handling
  - Centralized getSplunkBaseUrl() helper for all test files
affects: [CI/CD integration, smoke testing, deployment verification]

# Tech tracking
tech-stack:
  added: []  # dotenv already installed in plan 01
  patterns: ["Centralized config env vars via getSplunkBaseUrl()", "Self-contained create->delete test pattern for CRUD E2E"]

key-files:
  created:
    - tests/test-playwright-config.js — Unit tests for playwright.config.js Docker URL configuration
  modified:
    - playwright.config.js — Added dotenv + getSplunkBaseUrl helper with Splunk Docker target defaults
    - tests/smoke-test.spec.js — Expanded from 1 test to 7 tests (CRUD + auth error + conflict error)

key-decisions:
  - "Dotenv loaded at config top so all Playwright tests inherit SPLUNK_HOST/SPLUNK_PORT automatically"
  - "getSplunkBaseUrl attached as config property for both fixture use and external require()"
  - "CRUD E2E tests use create→delete self-contained pattern (test-{timestamp} prefix) to leave no residual state"
  - "Auth failure test navigates directly to app URL and verifies redirect to login page"

patterns-established:
  - "Config env var centralization: playwright.config.js is single source of truth for Splunk host/port"
  - "Self-cleaning E2E tests: each CRUD test creates its own test data and deletes it before completion"

requirements-completed: [D-04, D-05, D-06]

# Metrics
duration: ~5min
completed: 2026-04-28T19:34Z
---

# Phase 1.5 Plan 02: Playwright CRUD Test Suite Summary

**Playwright Docker target configuration and 7-test E2E suite covering credential CRUD operations with auth failure and conflict error scenarios against running Splunk instance**

## Performance

- **Duration:** ~5 min (19:30–19:34 UTC)
- **Started:** 2026-04-28T19:30:09Z
- **Completed:** 2026-04-28T19:34:22Z
- **Tasks:** 2 (both TDD, autonomous)
- **Files modified:** 3 (1 created, 2 modified)

## Accomplishments
- Centralized Docker target URL configuration via `getSplunkBaseUrl()` with env var override and sensible defaults (127.0.0.1:8000)
- Expanded E2E test coverage from 1 smoke test to 7 tests covering full CRUD lifecycle plus error handling
- Self-contained test pattern: each CRUD test creates and deletes its own data (`test-{timestamp}` prefix), preventing residual state

## Task Commits

Each task was committed atomically:

1. **Task 1 (TDD): Docker target URL in playwright config** — `9a72db7` (test) → `c6b02e4` (feat)
   - RED: Unit tests for getSplunkBaseUrl defaults and env var override
   - GREEN: dotenv loading + helper function export in playwright.config.js

2. **Task 2 (TDD): CRUD E2E test suite expansion** — `713a82d` (feat)
   - smoke-test.spec.js expanded from 1 to 7 tests

## Files Created/Modified
- `playwright.config.js` — Added dotenv loading, getSplunkBaseUrl() helper with 127.0.0.1:8000 defaults, env var override via SPLUNK_HOST/SPLUNK_PORT
- `tests/test-playwright-config.js` — Node test suite (5 tests) validating getSplunkBaseUrl defaults and env var behavior
- `tests/smoke-test.spec.js` — 7 Playwright E2E tests: table load (preserved), credential listing, create with cleanup, edit flow, delete with confirmation modal, auth failure redirect, duplicate conflict error

## Decisions Made
- **Centralized URL config**: playwright.config.js is the single source of truth for Splunk Docker target. Tests access it via `process.env` (dotenv) or `getSplunkBaseUrl()` helper.
- **No new dependencies**: dotenv was already installed from plan 01; no additional packages needed.
- **Self-cleaning tests**: Each CRUD test creates a credential with `test-{timestamp}` name and deletes it before completing, ensuring no residual state across test runs.

## Deviations from Plan

None — plan executed exactly as written. All threat model mitigations (T-1.5-04: .env in .gitignore; T-1.5-05: test-{timestamp} prefix) were verified and already satisfied.

## Issues Encountered
- Node.js `node:test` requires explicit `require('node:test')` for `describe/test/beforeEach/afterEach` — initial test used plain `describe()` which failed with `ReferenceError`. Fixed by importing from `node:test` module.
- `require.resolve('./playwright.config.js')` in tests directory resolved relative to the test file path, not project root — fixed to `../playwright.config.js`.

## Threat Model Verify
- **T-1.5-04** (Information Disclosure): `.env` already listed in `.gitignore` ✅
- **T-1.5-05** (Tampering on live data): All CRUD tests use `test-{timestamp}` prefix and self-delete after each test ✅
- **T-1.5-06** (Elevation of Privilege): Tests run against local Docker Splunk only — accepted risk per plan scope

## Next Phase Readiness
- E2E test infrastructure ready for CI/CD integration
- `getSplunkBaseUrl()` available as shared utility for future test plans
- All 7 tests registered and parse correctly via `npx playwright test --list`
- Tests require running Splunk Docker instance (127.0.0.1:8000) to execute

---
*Phase: 01.5-build-deployment*
*Completed: 2026-04-28*
