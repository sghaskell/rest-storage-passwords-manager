---
phase: 1.5-build-deployment
plan: 03
type: execute
wave: 2
depends_on: [01.5-01, 01.5-02]
files_modified:
  - package.json
autonomous: false
requirements: [D-07, D-08, D-10]
must_haves:
  truths:
    - "Production build succeeds with npm run build and outputs bundle.js"
    - "bundle.js size is under 2MB uncompressed (D-10)"
    - "Legacy password-crud.js exists alongside react/bundle.js (D-07)"
    - ".spl package can be generated containing appserver/static/default directories"
  artifacts:
    - path: "appserver/static/react/bundle.js"
      provides: "Production-optimized React bundle output"
      min_lines: 5
    - path: "package.json"
      provides: "Build verification and packaging scripts"
      contains: ["build", "package"]
  key_links:
    - from: "webpack.config.js" (modified by Plan 01)
      to: "appserver/static/react/bundle.js"
      via: "npm run build reads webpack.config.js, writes bundle.js"
      pattern: "\"build\": \"webpack --mode production\""
---

<objective>
Verify production build succeeds post-hardening, validate bundle size stays under target, confirm legacy file co-existence, and test package generation.

Purpose: End-to-end verification that all D-01 through D-10 decisions produce a working, packaged Splunk app ready for deployment.
Output: Verified build output, size-checked bundle.js, confirmed .spl package script.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-build-deployment/01.5-CONTEXT.md
@webpack.config.js
@package.json
</context>

<interfaces>
<!-- Current package.json scripts -->
From package.json:
```json
"scripts": {
  "build": "webpack --mode production",
  "build:watch": "webpack --mode development --watch",
  "build:dev": "webpack --mode development",
  "package": "find appserver static default -type f | tar -czf rest-storage-passwords-manager.spl -T -",
  "test": "npx playwright test",
  "install": "./bin/install-spl.sh"
}
```

<!-- Legacy file location -->
From app structure:
- appserver/static/password-crud.js — legacy bundled JS (D-07: keep until v3.0 verified)
- appserver/static/react/bundle.js — new React bundle
- default/ directory — contains app.conf, metadata, etc.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Verify production build succeeds and check bundle size under 2MB per D-10</name>
  <files>package.json</files>
  <read_first>webpack.config.js, package.json, appserver/static/react/bundle.jsx</read_first>
  <behavior>
    - Test: npm run build exits 0 (production build succeeds)
    - Test: appserver/static/react/bundle.js exists after build
    - Test: bundle.js file size is under 2MB / 2097152 bytes uncompressed
  </behavior>
  <action>
    1. Run `npm run build` to execute production webpack build with the hardened config from Plan 01 (TerserPlugin, externals, no sourcemaps).

    2. Verify bundle.js exists at appserver/static/react/bundle.js. If it does not exist, check webpack output path configuration.

    3. Check bundle size:
       - Run: `stat --format='%s' appserver/static/react/bundle.js` to get file size in bytes
       - Compare against 2097152 bytes (2MB uncompressed per D-10)
       - Log the actual size for record-keeping

    4. Verify console.* statements are NOT present in production bundle:
       - Run: `grep -c "console\.\(log\|error\)" appserver/static/react/bundle.js` should return 0 (D-09)
    
    5. Add a prebuild verification script to package.json for CI-friendly build+size check:
       ```json
       "check:bundle-size": "node -e \"const fs=require('fs'); const size=fs.statSync('appserver/static/react/bundle.js').size; if(size > 2097152){console.error('FAIL: bundle.js ('+size+' bytes) exceeds 2MB limit');process.exit(1)} else {console.log('PASS: bundle.js '+size+' bytes (under 2MB)')}\""
       ```

    Note: The externals config from Plan 01 (react/react-dom not bundled) will make the bundle significantly smaller than it was before. D-10's target is "under 2MB uncompressed for modern Splunk browser environments" — with React/ReactDOM externalized, this should be easily met.
  </action>
  <acceptance_criteria>
    - npm run build exits 0 (no errors)
    - appserver/static/react/bundle.js exists after build
    - bundle.js size <= 2097152 bytes (stat output confirms)
    - grep for console.log or console.error in bundle.js returns 0 matches (D-09)
    - "check:bundle-size" script is added to package.json
  </acceptance_criteria>
  <verify>
    <automated>npm run build && npm run check:bundle-size</automated>
  </verify>
  <done>Production build succeeds, bundle.js under 2MB, no console.* in production output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify legacy file co-existence (D-07) and package generation works</name>
  <files>package.json</files>
  <read_first>appserver/static/password-crud.js, .gitignore</read_first>
  <behavior>
    - Test: appserver/static/password-crud.js exists (legacy file not removed during optimization)
    - Test: react/bundle.js exists alongside password-crud.js
    - Test: npm run package creates rest-storage-passwords-manager.spl
  </behavior>
  <action>
    1. Verify legacy file still exists at appserver/static/password-crud.js (D-07: keep until v3.0 verified). This is a read-only check — webpack optimization in Plan 01 only modifies config, it does not delete source files.

    2. Verify both files co-exist in the static directory structure:
       ```bash
       ls -la appserver/static/password-crud.js appserver/static/react/bundle.js
       ```

    3. Run `npm run package` to generate rest-storage-passwords-manager.spl and verify it succeeds:
       ```bash
       npm run package && ls -lh rest-storage-passwords-manager.spl
       ```
    
    4. Add a verification script for dual-file co-existence:
       ```json
       "verify:legacy": "node -e \"const fs=require('fs'); const files=['appserver/static/password-crud.js','appserver/static/react/bundle.js']; let missing=files.filter(f=>!fs.existsSync(f)); if(missing.length){console.error('MISSING:',missing);process.exit(1)} else console.log('OK: legacy + react bundle both present')\""
       ```

    5. Check that .gitignore excludes .env (security) and build artifacts like node_modules:
       - If .gitignore does NOT contain `.env`, add it
       - Verify `.spl` is in .gitignore (generated artifact, not committed)
  </action>
  <acceptance_criteria>
    - appserver/static/password-crud.js exists (not deleted by build hardening)
    - appserver/static/react/bundle.js exists (generated by build)
    - npm run package creates rest-storage-passwords-manager.spl without error
    - "verify:legacy" script added to package.json
    - .env is in .gitignore
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:legacy && npm run package</automated>
  </verify>
  <done>Legacy file co-exists with React bundle, packaging generates valid .spl, security checks on .gitignore.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of end-to-end build, CRUD tests, and package in live Splunk</name>
  <what-built>Production build hardened, Playwright tests written, legacy file verified, package script working. Human verifies end-to-end against running Splunk Docker instance.</what-built>
  <how-to-verify>
    1. **Ensure Splunk is running locally on port 8000** (Docker or local install). If not running, follow install instructions in `.env` / `bin/install-spl.sh`.
    
    2. **Run the production build**: `npm run build` — verify it exits 0 and bundle.js is generated under 2MB.

    3. **Run Playwright CRUD tests**: `npx playwright test --headed` (use --headed to observe browser interaction):
       - Confirm credential listing loads from React dashboard
       - Confirm credential create works (form submit → table update)
       - Confirm credential edit/update works
       - Confirm credential delete + confirmation modal works
       - Note any failures or UI issues

    4. **Install the .spl package in Splunk** (optional, if local Splunk is available):
       - `npm run package` to generate .spl
       - Install via Splunk Web: Settings -> Add-ons -> Upload app
       - Navigate to dashboard, verify React app loads and CRUD works

    5. **D-08 note**: Version bumping and documentation updates follow standard gitflow "when ready to release". The version in app.conf is currently 2.1.1 — DO NOT bump to 3.0.0 yet (D-08: "docs created as part of version bump, not pre-built"). This checkpoint confirms the build is READY for version bump when the team decides to ship.

    After verification, type "approved" or list any issues found with specific test names and expected vs actual behavior.
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific test failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .spl package → Splunk runtime | Package contents are loaded by Splunk — must not contain secrets or build artifacts with embedded credentials |
| npm scripts → CI execution | Build/package scripts run in pipeline context; should not execute arbitrary commands |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-------------|-----------------|
| T-1.5-07 | Repudiation | .sl package contents | mitigate | "verify:legacy" script ensures only expected files present — no unexpected additions to package |
| T-1.5-08 | Information Disclosure | Build artifacts with env vars | mitigate | .env excluded from .gitignore; bundle.js should not contain hardcoded credentials (no process.env in source) |
| T-1.5-09 | Denial of Service | Bundle size target exceeded | accept | 2MB limit (D-10) is conservative for Splunk browsers — if exceeded, check externals config and minification |
</threat_model>

<verification>
Run `npm run build && npm run check:bundle-size` to verify production build + size target.
Run `npm run verify:legacy` to confirm legacy + React bundle co-existence.
Run `npx playwright test` against running Splunk Docker instance (requires Splunk on localhost:8000 with valid .env credentials).
Verify `.gitignore` contains `.env` and `*.spl`.
</verification>

<success_criteria>
- [ ] Production build (`npm run build`) succeeds without errors
- [ ] bundle.js file size under 2MB uncompressed (stat confirms <= 2097152 bytes)
- [ ] console.log/console.error absent from production bundle.js output
- [ ] Legacy password-crud.js co-exists alongside react/bundle.js in static directory
- [ ] `npm run package` generates valid rest-storage-passwords-manager.spl
- [ ] Playwright tests pass against running Splunk Docker instance (human-verified in checkpoint)
- [ ] .env excluded from .gitignore, .spl excluded from source control
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-build-deployment/01.5-03-SUMMARY.md`
</output>
