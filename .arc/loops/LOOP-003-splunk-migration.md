# LOOP.md — Session 3
**Agent:** Qwen3.6-27B

## Goal
Migrate the React app from bespoke DOM and inline styles to @splunk/react-ui components, fixing pagination in the process. Determine whether any other bespoke JS can be eliminated with Splunk React components and pull into scope.

## Definition of done
1. All UI elements (table, modals, forms, layout) and any other identified bespoke components sourced from @splunk/react-ui with no bespoke table/modal/form elements remaining.
2. Pagination renders all credentials correctly; full CRUD and bulk operations verified functional post-migration.

## Scope additions confirmed this session
- Use arc-build process: produce spec via writing-plans before any coding begins. Big refactor requires decomposed subagent work, not ad-hoc coding.

## Out of scope this session
- Dark theme beyond what Splunk design tokens provide out of box
- Unit test suite (DISCOVERIES.md parked)
- Error message dangerouslySetInnerHTML sanitization (DISCOVERIES.md parked)

---

## Superpowers Planning Input
*Generated by arc-build — do not edit manually*

### Spec input
DoD only (no separate spec file; writing-plans to decompose from LOOP.md DoD)

### Hard Rails (from INTENT.md — non-negotiable)
- Always use the JS version (password-crud.js) as reference if you get stuck; line numbers matter (e.g., L511-554 is update flow)
- Always check Splunk's React docs - https://splunkui.splunk.com/Packages/react-ui/Overview
- Always ask questions if unclear of intent, don't assume you know what I want

### Critical Patterns (must not be violated)
- REST call behavior must match password-crud.js exactly — behavioral parity is non-negotiable
- ACL path must go through `/configs/conf-passwords/credential:${realm}:${username}:`, not `${rest_uri}/acl` (returns 404)
- User-scoped credentials require temporary sharing bump (`user→app→fetch→user`) for password reveal and deletion
- Update sequence: ACL bump sharing=app → POST password only → `/move` if app changed → final ACL
- Delete sequence: per-credential ACL bump → DELETE via explicit owner/app path
- Realm field immutable post-create: disabled in edit mode; REST API doesn't allow modification
- Auth strategy: cookie + CSRF via `splunkd/__raw` proxy, NOT .env credentials

### Resources (verify here when uncertain)
- JS reference: password-crud.js (full standalone working implementation)
- Splunk React UI docs: https://splunkui.splunk.com/Packages/react-ui/Overview
- Vercel React best practices skill (local)

### Execution mode
- Strategy: subagent-driven-development (autonomous)
- Brainstorming: SUPPRESSED — intent captured in INTENT.md
- Task decomposition: writing-plans owns this — do not re-derive from DoD
- Context per subagent: fresh (do not pass session history)
- On conflict with INTENT.md: stop and surface to operator, do not resolve autonomously
