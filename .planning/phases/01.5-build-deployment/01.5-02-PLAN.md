---
phase: 1.5-build-deployment
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.js
  - tests/smoke-test.spec.js
autonomous: true
requirements: [D-04, D-05, D-06]
must_haves:
  truths:
    - "Playwright test suite includes credential listing test (GET operation)"
    - "Playwright test suite includes credential create test (POST operation)"
    - "Playwright test suite includes credential update test (PUT operation)"
    - "Playwright test suite includes credential delete test (DELETE operation)"
    - "Tests include error handling for auth failures (401/403), 404s, and 400s"
    - "Playwright config targets Docker Splunk host (process.env.SPLUNK_HOST or 127.0.0.1:8000)"
  artifacts:
    - path: "tests/smoke-test.spec.js"
      provides: "Automated CRUD tests via Playwright against running Splunk instance"
      min_lines: 100
    - path: "playwright.config.js"
      provides: "Extended playwright config with Docker target URL configuration"
      contains: ["SPLUNK_HOST", "testDir"]
  key_links:
    - from: "tests/smoke-test.spec.js"
      to: "appserver/static/react/api.js"
      via: "Playwright REST API calls using splunkd/__raw base paths"
      pattern: "splunkd/__raw.*storage/passwords"
    - from: "playwright.config.js"
      to: ".env"
      via: "dotenv for SPLUNK_ADMIN_USER/SPLUNK_ADMIN_PASSWORD env vars"
      pattern: "[Ss]plunk.*[Hh]ost|[Aa]dmin"
---

<objective>
Build Playwright CRUD test suite for automated verification of credential listing, create, update, and delete operations with error handling.

Purpose: Verify all core API operations work correctly against a running Splunk Docker instance (happy paths + critical errors) without manual testing overhead.
Output: Updated playwright.config.js with Docker target URL; expanded tests/smoke-test.spec.js with CRUD coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.5-build-deployment/01.5-CONTEXT.md
@playwright.config.js
@appserver/static/react/api.js
</context>

<interfaces>
<!-- Current playwright.config.js -->
From playwright.config.js:
```javascript
const { defineConfig } = require('@playwright/test');

module.exports = defineConfig({
  testDir: './tests',
  timeout: 60000,
  use: {
    headless: true,
    ignoreHTTPSErrors: true,
    viewport: { width: 1280, height: 720 },
  },
  reporter: 'line',
});
```

<!-- Current smoke-test.spec.js structure -->
From tests/smoke-test.spec.js (summary):
- 1 test: "should load the credentials table after login" 
- Logs into Splunk via username/password form
- Navigates to app home, then to credential_management dashboard
- Verifies #credential-manager-app container is visible and table renders

<!-- Current .env contents -->
```
SPLUNK_ADMIN_USER=admin
SPLUNK_ADMIN_PASSWORD=password
```

<!-- API.js key exports from bundle.jsx -->
From api.js (referenced by CONTEXT.md):
```
API.getAllCredentials()     // GET /servicesNS/{owner}/{app}/storage/passwords/
API.createCredential(name, clearpass, realm)  // POST with CSRF
API.updateCredential(name, read, write, owner, sharing?, app?)  // PUT
API.deleteCredential(name, sharing?, app?)    // DELETE
API.getApps()               // GET /services/apps/local/
API.getUsers()              // GET /services/authentication/current-context/
API.getRoles()              // GET admin/roles
```

<!-- Splunk REST patterns -->
- CSRF token (X-Splunk-Form-Key): Required for POST/PUT/DELETE — captured from /servicesNS/{user}/{app}/storage/passwords/ or similar endpoint response headers
- Authentication: Cookie-based (login form) or session key header
- URL format: {host}/en-US/app/rest-storage-passwords-manager/credential_management
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Docker target URL to playwright.config.js per D-04</name>
  <files>playwright.config.js</files>
  <read_first>playwright.config.js, .env</read_first>
  <behavior>
    - Test: SPLUNK_HOST env var defaults to '127.0.0.1' if not set
    - Test: SPLUNK_PORT env var defaults to '8000' if not set  
    - Test: Global baseURL or global setup exposes splunkBaseUrl for test use
  </behavior>
  <action>
    1. Add `require('dotenv').config()` at the top of playwright.config.js.
    2. Export a helper function or constant `getSplunkHost()` that reads from environment:
       - `process.env.SPLUNK_HOST || '127.0.0.1'` for host
       - `process.env.SPLUNK_PORT || '8000'` for port
    3. Add global setup or testUse configuration that makes the Splunk base URL available to all test files via test fixtures or module-level constants.
    4. Keep existing config (headless, ignoreHTTPSErrors, viewport, reporter) unchanged.

    The goal is centralized Docker target URL so tests can configure `http://{host}:{port}/...` without hardcoding values in each test file. D-04 says "Playwright for Docker host testing only" — no CI/CD or remote target needed.
  </action>
  <acceptance_criteria>
    - grep "dotenv" playwright.config.js returns a match
    - grep "SPLUNK_HOST\|getSplunkHost\|baseURL" playwright.config.js returns at least one match
    - Default values '127.0.0.1' and '8000' are present in the file
    - Existing reporter, headless, viewport config is preserved
  </acceptance_criteria>
  <verify>
    <automated>node -e "require('./playwright.config.js'); console.log('CONFIG OK')"</automated>
  </verify>
  <done>playwright.config.js loads dotenv and exposes Splunk Docker target URL (host:port) with defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand smoke-test.spec.js with CRUD test coverage per D-05/D-06</name>
  <files>tests/smoke-test.spec.js</files>
  <read_first>tests/smoke-test.spec.js, appserver/static/react/api.js</read_first>
  <behavior>
    - Test: File contains test for credential listing (search for 'list' or 'GET' in describe/test name)
    - Test: File contains test for credential creation
    - Test: File contains test for credential update/edit  
    - Test: File contains test for credential deletion with confirmation
    - Test: File contains tests for error scenarios (401/403 auth, 404 not found, 400 validation)
  </behavior>
  <action>
    Expand existing smoke-test.spec.js to add CRUD automation tests. The current file has one test that logs in and verifies the React app table renders. Add the following additional tests (preserve the existing test):

    **Test Group: CRUD Operations** (uses DOM interactions on the React dashboard)
    
    1. **Credential Create**: Fill out the create credential form (username, password, read roles, write roles), submit, verify new credential appears in table. Use the Splunk login flow from existing test.
    
    2. **Credential Update**: Click edit on a credential, modify read/write roles or app assignment, save, verify updated values appear in the table row.
    
    3. **Credential Delete**: Click delete on a credential, confirm in the modal (ConfirmDeleteModal), verify credential no longer appears in table.

    **Test Group: Error Scenarios** (D-06: critical error handling)
    
    4. **Auth failure test**: Attempt to access React dashboard without login, verify user is redirected to Splunk login page (URL matches /account/login pattern).
    
    5. **Invalid input for create**: Submit form with duplicate credential name, verify conflict error message displayed (e.g., "already exists" or "conflict").

    Implementation notes:
    - Use the existing login flow from the current test as a reusable pattern (page.goto → fill username/password → click Sign In)
    - Target React component elements by text content, button labels, or CSS class selectors visible in browser DOM
    - The credential form fields include: Username, Password, Read roles (comma-separated), Write roles, Sharing selector, App selector
    - For cleanup tests: create and delete the same credential within a single test to leave no residual state
    
    D-06 scope: "happy paths + critical error scenarios" — do NOT add exhaustive edge case coverage. Focus on the 5 tests above.
  </action>
  <acceptance_criteria>
    - grep -c "test(" tests/smoke-test.spec.js returns at least 5 test definitions (existing load test + 4 new)
    - grep -iE "creat|list|edit|delete|auth" tests/smoke-test.spec.js matches each CRUD verb at least once
    - Existing 'should load the credentials table after login' test is preserved (not removed)
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --list 2>&1 | head -20</automated>
  </verify>
  <done>smoke-test.spec.js contains: credential listing, create, update, delete tests + auth failure and conflict error tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundatories

| Boundary | Description |
|----------|-------------|
| test env vars → Splunk credentials | .env contains admin credentials used by Playwright for login/testing |
| REST API calls via playwright | Tests interact with live Splunk instance — mutations affect real data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-------------|-----------------|
| T-1.5-04 | Information Disclosure | .env credentials | mitigate | Ensure .gitignore excludes .env; add `.env` to gitignore if not already present |
| T-1.5-05 | Tampering | CRUD tests on live data | mitigate | Tests create and delete their own credentials (self-contained) — use unique test-specific credential names with 'test-' prefix |
| T-1.5-06 | Elevation of Privilege | admin login in tests | accept | Tests run against local Docker Splunk only (D-04), not production — acceptable risk for development testing |
</threat_model>

<verification>
Run `npx playwright test --list` to verify all test cases are registered and discoverable.
Check that .env is listed in .gitignore (if not, add it).
Verify smoke-test.spec.js contains at least 5 tests covering CRUD + errors.
</verification>

<success_criteria>
- [ ] playwright.config.js loads dotenv and exposes Docker target URL (host:port with defaults 127.0.0.1:8000)
- [ ] smoke-test.spec.js contains tests for: credential list, create, update, delete
- [ ] Smoke-test.spec.js contains tests for: auth failure, conflict/duplicate error
- [ ] Existing table-load test preserved (not removed during expansion)
- [ ] npx playwright test --list shows all tests without parse errors
- [ ] .env is in .gitignore (prevent credential leakage)
</success_criteria>

<output>
After completion, create `.planning/phases/01.5-build-deployment/01.5-02-SUMMARY.md`
</output>
