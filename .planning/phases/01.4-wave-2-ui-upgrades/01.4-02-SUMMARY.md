---
phase: 01.4-wave-2-ui-upgrades
plan: 02
subsystem: api
tags: [splunk, acl, rest-api, sharing]

requires:
  - phase: 01.4-wave-2-ui-upgrades
    provides: CredentialForm emitting sharing in onSave payload (plan 01.4-01)
provides:
  - createCredential with two-step ACL pattern for user-scoped credentials
  - updateCredential with pre-bump ACL pattern matching legacy L522-523
  - deleteCredential with pre-delete ACL bump for user-scoped credentials
  - bundle.jsx handlers passing data.sharing through all CRUD API calls
affects: [future wave phases, credential parity verification]

tech-stack:
  added: []
  patterns: [two-step ACL write, pre-bump ACL pattern, pre-delete ACL bump]

key-files:
  created: []
  modified:
    - appserver/static/react/api.js
    - appserver/static/react/bundle.jsx

key-decisions:
  - "Two-step ACL write mirrors legacy password-crud.js L478-481 exactly"
  - "deleteCredential signature uses (name, realm, app, sharing) — app needed for buildAclPath in pre-bump"
  - "All CRUD functions default sharing='app' for backward compatibility with existing callers"

patterns-established:
  - "Two-step ACL: initial app-scoped write, then final write with actual sharing value"
  - "Pre-bump on update: always set sharing=app before final ACL to normalize splunkd URI behavior"
  - "Pre-delete bump: bump user-scoped credentials to app + '*' perms before deletion"

requirements-completed: [GAP-C05, GAP-C04]

duration: 2min
completed: 2026-04-28
---

# Phase 01.4 Plan 02: API Sharing Support Summary

**Two-step ACL pattern for user-scoped credentials threaded through all CRUD operations — createCredential, updateCredential, and deleteCredential accept sharing parameter with legacy-compatible splunkd dance.**

## Performance

- **Duration:** ~2 min
- **Started:** 2026-04-28T18:29:36Z
- **Completed:** 2026-04-28T18:31:07Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- createCredential implements two-step ACL write for user-scoped credentials (legacy L478-481)
- updateCredential implements pre-bump ACL pattern before final write (legacy L522-523)
- deleteCredential implements pre-delete ACL bump preventing failures on user-scoped credentials (legacy L572-576)
- All three bundle.jsx handlers (create/update/delete) thread `data.sharing` into API calls

## Task Commits

Each task was committed atomically:

1. **Task 1: Add sharing parameter to createCredential + two-step ACL pattern** - `2d6791f` (feat)
2. **Task 2: Add sharing to updateCredential/deleteCredential + wire bundle handlers** - `d65f19f` (feat)

## Files Modified
- `appserver/static/react/api.js` — createCredential/updateCredential/deleteCredential all accept sharing parameter with two-step/pre-bump ACL patterns
- `appserver/static/react/bundle.jsx` — handleCreateCredential, handleUpdateCredential, handleDeleteCredential pass data.sharing || 'app' through API calls

## Decisions Made
- deleteCredential signature: `(name, realm, app, sharing)` — `app` parameter added so buildAclPath has the correct app context for the pre-delete bump. Defaults to credential's stored `app` value from bundle.jsx.
- All functions default `sharing = 'app'` ensuring existing test code and any callers without sharing data continue working identically.

## Deviations from Plan

None - plan executed exactly as written. The three sub-actions (A: updateCredential, B: bundle wiring, C: deleteCredential) were all implemented per specification.

## Issues Encountered
- Minor indentation issue introduced during edit on line 109 of bundle.jsx — caught and fixed inline during verification.

## Known Stubs
None - no stubs introduced in this plan.

## Threat Surface Scan
No new threat surface. The sharing parameter flows through existing trusted API paths and is passed to splunkd ACL endpoints with the same authentication/CSRF protection as all other operations.

## Next Phase Readiness
- API layer fully supports sharing parameter through all CRUD operations
- Legacy two-step ACL pattern verified against password-crud.js reference
- Ready for subsequent wave plans to consume sharing-aware API surface

---
*Phase: 01.4-wave-2-ui-upgrades*
*Completed: 2026-04-28*
